# Changelog

## [1.1.0] - 2026-04-17
- Added optional tool `tools/media-import.sh` for importing an image and setting featured image via `./wp.sh`.
- Updated `install.sh` to copy optional tool into target project (`./tools/media-import.sh`).
- Updated `README.md` with optional tool usage.

## [1.0.0] - 2026-04-17
- Initial simplified package.
- Renamed package to `wp-cli-config`.
- Flattened structure to core files for WP-CLI connection setup.
- Added `install.sh` for quick reuse across projects.
